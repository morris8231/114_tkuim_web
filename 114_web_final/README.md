# PhotoMission - 攝影任務學習系統

一個基於章節的攝影學習平台，結合任務系統、作品提交、經驗值成長機制，以及完整的用戶管理和管理員分析功能。

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![Node](https://img.shields.io/badge/node-%3E%3D14.0.0-green.svg)
![License](https://img.shields.io/badge/license-ISC-orange.svg)

---

## 🎯 專案目標

### 主要目標
本專案旨在建立一個**互動式攝影學習平台**，讓學習者能夠：
1. **系統化學習**：透過 8 個精心設計的章節，從基礎到進階循序漸進
2. **實作導向**：每個章節包含實際任務，強調「做中學」
3. **即時回饋**：上傳作品後獲得經驗值，追蹤學習進度
4. **社群互動**：公開作品畫廊，與其他學習者交流

### 解決的問題
- ❌ 傳統攝影課程缺乏實作練習
- ❌ 學習進度難以追蹤
- ❌ 缺少作品管理和回顧機制
- ❌ 學習動機不足

### 目標用戶
- 📱 **手機攝影愛好者**：想提升手機拍攝技巧
- 📷 **相機初學者**：剛開始學習攝影的新手
- 🎓 **攝影課程學生**：需要系統化學習路徑

---

## 📋 目錄

- [功能特色](#功能特色)
- [技術架構](#技術架構)
- [系統需求](#系統需求)
- [安裝步驟](#安裝步驟)
- [環境設定](#環境設定)
- [啟動專案](#啟動專案)
- [專案結構](#專案結構)
- [API 文件](#api-文件)
- [管理員功能](#管理員功能)
- [常見問題](#常見問題)

---

## ✨ 功能特色

### 🎓 學習系統
- **8 個攝影章節**：從基礎到進階的完整攝影課程
- **任務系統**：每個章節包含多個實作任務
- **YouTube 教學影片**：整合線上教學資源
- **進度追蹤**：自動記錄學習進度和完成狀態

### 📸 作品管理
- **照片上傳**：支援多張照片上傳（最多 5 張）
- **作品展示**：個人作品集和公開畫廊
- **CRUD 操作**：完整的新增、查看、編輯、刪除功能
- **每月回顧**：按月份整理作品集

### 👤 用戶系統
- **Email 驗證**：註冊後需驗證 Email 才能使用
- **JWT 認證**：安全的 Token-based 身份驗證
- **個人資料**：暱稱、Email、設備類型設定
- **經驗值系統**：完成任務獲得 XP 和等級提升

### 🔧 管理員功能
- **分析儀表板**：即時統計數據
  - 網站點擊次數（追蹤所有用戶互動）
  - 活躍帳戶數（30 天內登入）
  - 總註冊用戶數
  - 已驗證用戶數
- **獨立管理頁面**：`/admin.html`
- **自動刷新**：每 30 秒更新數據

### 📧 Email 功能
- **驗證郵件**：註冊後自動發送
- **重發驗證**：支援重新發送驗證郵件（5 分鐘冷卻）
- **聯絡表單**：用戶可透過表單聯繫管理員

---

## 🛠 技術架構

### 後端
- **Node.js** + **Express.js** - Web 框架
- **MongoDB** + **Mongoose** - 資料庫
- **JWT** - 身份驗證
- **bcryptjs** - 密碼加密
- **Multer** - 檔案上傳
- **Nodemailer** - Email 發送

### 前端
- **Vanilla JavaScript** - 無框架，純 JS
- **SPA 架構** - 單頁應用程式
- **Responsive Design** - 響應式設計
- **Modern CSS** - 現代化樣式

### 安全性
- **JWT Token** 認證
- **密碼 Hash** (bcrypt)
- **Email 驗證** 機制
- **Role-based Access Control** (RBAC)
- **Middleware 保護** 敏感路由

### 技術選擇原因

#### 為什麼選擇 Node.js + Express.js？
- ✅ **JavaScript 全端開發**：前後端使用同一語言，降低學習成本
- ✅ **非同步處理**：適合處理檔案上傳、Email 發送等 I/O 密集操作
- ✅ **豐富生態系**：npm 套件完整，開發效率高
- ✅ **輕量快速**：Express.js 簡潔靈活，適合中小型專案

#### 為什麼選擇 MongoDB？
- ✅ **Schema 彈性**：文件導向資料庫，適合快速迭代
- ✅ **JSON 格式**：與 JavaScript 無縫整合
- ✅ **易於擴展**：水平擴展能力強
- ✅ **開發友善**：Mongoose ODM 提供優雅的資料建模

#### 為什麼選擇 Vanilla JavaScript？
- ✅ **無框架依賴**：減少專案複雜度和打包體積
- ✅ **學習基礎**：強化 JavaScript 原生能力
- ✅ **效能優異**：無額外框架開銷
- ✅ **完全掌控**：對 DOM 操作有完整控制權

#### 為什麼選擇 JWT？
- ✅ **無狀態認證**：不需要 session 儲存，易於擴展
- ✅ **跨域支援**：適合 SPA 和 API 架構
- ✅ **自包含**：Token 包含所有必要資訊
- ✅ **標準化**：業界廣泛採用的認證方案

---

## 🎨 設計模式 (Design Patterns)

本專案採用多種設計模式來提升程式碼的可維護性、可測試性和可擴展性。

### 後端設計模式

#### 1. Repository Pattern (資料存取層模式)

**目的**：將資料存取邏輯與業務邏輯分離

**實作位置**：`repositories/` 目錄
- `UserRepository.js` - 用戶資料 CRUD
- `ChapterRepository.js` - 章節資料 CRUD
- `TaskRepository.js` - 任務資料 CRUD
- `SubmissionRepository.js` - 作品資料 CRUD

**優點**：
- ✅ 集中管理資料庫操作
- ✅ 易於測試（可 mock Repository）
- ✅ 資料庫切換時只需修改 Repository 層
- ✅ 避免重複的查詢邏輯

**範例**：
```javascript
// UserRepository.js
class UserRepository {
    async findUserByEmail(email) {
        return await User.findOne({ email });
    }
    
    async createUser(userData) {
        return await User.create(userData);
    }
}
```

---

#### 2. Service Pattern (服務層模式)

**目的**：封裝業務邏輯，協調多個 Repository 的操作

**實作位置**：`services/` 目錄
- `AuthService.js` - 認證業務邏輯
- `ChapterService.js` - 章節業務邏輯
- `TaskService.js` - 任務業務邏輯
- `SubmissionService.js` - 作品業務邏輯
- `EmailService.js` - Email 發送邏輯

**優點**：
- ✅ 業務邏輯集中管理
- ✅ 可重複使用的業務方法
- ✅ 易於進行單元測試
- ✅ 保持 Controller 簡潔

**範例**：
```javascript
// AuthService.js
class AuthService {
    async registerUser(nickname, email, password) {
        // 1. 檢查用戶是否存在
        const existingUser = await UserRepository.findUserByEmail(email);
        if (existingUser) throw new Error('User exists');
        
        // 2. Hash 密碼
        const hashedPassword = await bcrypt.hash(password, 10);
        
        // 3. 建立用戶
        const user = await UserRepository.createUser({...});
        
        // 4. 發送驗證郵件
        await EmailService.sendVerificationEmail(email, token);
        
        return user;
    }
}
```

---

#### 3. Singleton Pattern (單例模式)

**目的**：確保某個類別只有一個實例

**實作位置**：
- `models/Analytics.js` - 分析數據（全域唯一）
- `config/db.js` - 資料庫連線（全域唯一）

**優點**：
- ✅ 節省資源（只建立一次）
- ✅ 全域狀態管理
- ✅ 避免重複初始化

**範例**：
```javascript
// Analytics.js
AnalyticsSchema.statics.getAnalytics = async function() {
    let analytics = await this.findOne();
    if (!analytics) {
        analytics = await this.create({});
    }
    return analytics; // 永遠返回同一個文檔
};
```

---

### 前端設計模式

#### 4. Observer Pattern (觀察者模式)

**目的**：實現事件驅動的狀態管理

**實作位置**：`public/js/eventBus.js`

**優點**：
- ✅ 元件間鬆耦合通訊
- ✅ 事件驅動架構
- ✅ 易於擴展新功能
- ✅ 狀態變化自動通知

**範例**：
```javascript
// eventBus.js
const eventBus = {
    events: {},
    
    on(event, callback) {
        if (!this.events[event]) this.events[event] = [];
        this.events[event].push(callback);
    },
    
    emit(event, data) {
        if (this.events[event]) {
            this.events[event].forEach(cb => cb(data));
        }
    }
};

// 使用範例
eventBus.on('userLoggedIn', (user) => {
    console.log('User logged in:', user);
    updateUI(user);
});

eventBus.emit('userLoggedIn', userData);
```

---

#### 5. Factory Pattern (工廠模式)

**目的**：統一建立和管理 UI 元件

**實作位置**：`public/js/componentFactory.js`

**優點**：
- ✅ 集中管理元件建立邏輯
- ✅ 保持元件結構一致
- ✅ 易於修改元件樣式
- ✅ 減少重複程式碼

**範例**：
```javascript
// componentFactory.js
const componentFactory = {
    createSubmissionCard(submission) {
        return `
            <div class="submission-card">
                <img src="${submission.photo}">
                <p>${submission.description}</p>
                <button onclick="editSubmission('${submission.id}')">編輯</button>
            </div>
        `;
    },
    
    createChapterCard(chapter) {
        return `
            <div class="chapter-card">
                <h3>${chapter.title}</h3>
                <p>${chapter.description}</p>
            </div>
        `;
    }
};

// 使用範例
const cardHTML = componentFactory.createSubmissionCard(data);
container.innerHTML = cardHTML;
```

---

### 設計模式架構圖

```
前端 (Frontend)
├── Observer Pattern (eventBus.js)
│   └── 事件驅動的狀態管理
└── Factory Pattern (componentFactory.js)
    └── 統一元件建立

後端 (Backend)
├── Repository Pattern (repositories/)
│   └── 資料存取層
├── Service Pattern (services/)
│   └── 業務邏輯層
└── Singleton Pattern (models/Analytics.js)
    └── 全域單一實例
```

---

### 為什麼使用這些設計模式？

1. **關注點分離 (Separation of Concerns)**
   - Repository 處理資料
   - Service 處理業務邏輯
   - Controller 處理 HTTP 請求

2. **可測試性 (Testability)**
   - 每一層都可以獨立測試
   - 可以輕鬆 mock 依賴

3. **可維護性 (Maintainability)**
   - 程式碼結構清晰
   - 修改影響範圍小

4. **可擴展性 (Scalability)**
   - 新增功能時遵循相同模式
   - 易於理解和擴展

---

## 💻 系統需求

- **Node.js**: >= 14.0.0
- **MongoDB**: >= 4.4
- **npm**: >= 6.0.0
- **Gmail 帳號**：用於發送驗證郵件（需要 App Password）

---

## 📦 安裝步驟

### 1. Clone 專案

```bash
git clone <repository-url>
cd 114_web_final
```

### 2. 安裝依賴套件

```bash
npm install
```

### 3. 設定環境變數

複製 `.env.example` 並重新命名為 `.env`：

```bash
cp .env.example .env
```

編輯 `.env` 檔案，填入以下資訊：

```env
# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/photomission

# JWT Secret (請使用強密碼)
JWT_SECRET=your_super_secret_jwt_key_here

# Admin Secret (用於建立管理員)
ADMIN_SECRET_KEY=your_admin_secret_key_here

# Email Configuration (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_FROM=PhotoMission <noreply@photomission.com>

# Server Configuration
PORT=3001
BASE_URL=http://localhost:3001
```

### 4. 設定 Gmail App Password

1. 前往 [Google Account Security](https://myaccount.google.com/security)
2. 啟用「兩步驟驗證」
3. 前往 [App Passwords](https://myaccount.google.com/apppasswords)
4. 選擇「郵件」和「其他（自訂名稱）」
5. 產生密碼並複製到 `.env` 的 `EMAIL_PASS`

### 5. 初始化資料庫

```bash
npm run init-db
```

這會建立：
- 8 個攝影章節
- 每個章節的任務
- 範例資料

### 6. 建立管理員帳號

```bash
node promote_admin.js <your-email@example.com>
```

---

## 🚀 啟動專案

### 開發模式（自動重啟）

```bash
npm run dev
```

### 正式模式

```bash
npm start
```

伺服器會在 `http://localhost:3001` 啟動

---

## 📁 專案結構

### 專案根目錄

```
114_web_final/
```

整個系統的根目錄，採用 **Full-Stack 單一專案架構**，前端與後端整合在同一個專案中。

---

### 🎨 前端 (Frontend)

#### `public/`
負責使用者介面與前端互動邏輯。

**內容包含**：

```
public/
├── index.html              # 主應用程式頁面 (SPA)
├── admin.html              # 管理員儀表板頁面
├── css/
│   └── styles.css          # 全域樣式表
└── js/
    ├── app.js              # 核心應用程式邏輯
    ├── eventBus.js         # Observer Pattern - 事件總線
    └── componentFactory.js # Factory Pattern - 元件工廠
```

**說明**：
- 採用 **Vanilla JavaScript** 實作 SPA (Single Page Application)
- 使用 **Observer Pattern** 進行狀態管理
- 使用 **Factory Pattern** 統一建立 UI 元件
- 無需打包工具，直接載入執行

---

### ⚙️ 後端 (Backend)

負責系統 API、資料處理與核心商業邏輯。

#### `routes/` - API 路由層
定義 HTTP 端點與請求處理。

```
routes/
├── auth.js     # 認證相關路由 (註冊/登入/驗證)
├── api.js      # 主要業務 API (章節/任務/作品)
└── admin.js    # 管理員專用 API (分析數據)
```

#### `middleware/` - 中介層
請求處理前的驗證與處理邏輯。

```
middleware/
├── auth.js             # JWT Token 驗證
├── admin.js            # 管理員權限檢查
├── verifiedOnly.js     # Email 驗證狀態檢查
└── pageViewTracker.js  # 頁面瀏覽追蹤
```

#### `services/` - 服務層 (Service Pattern)
封裝業務邏輯，協調多個 Repository。

```
services/
├── AuthService.js       # 認證業務邏輯
├── ChapterService.js    # 章節業務邏輯
├── TaskService.js       # 任務業務邏輯
├── SubmissionService.js # 作品業務邏輯
└── EmailService.js      # Email 發送邏輯
```

**說明**：
- 實作 **Service Pattern**
- 處理複雜的業務流程
- 協調多個 Repository 和外部服務

#### `repositories/` - 資料存取層 (Repository Pattern)
封裝資料庫操作，與業務邏輯分離。

```
repositories/
├── UserRepository.js       # 用戶資料 CRUD
├── ChapterRepository.js    # 章節資料 CRUD
├── TaskRepository.js       # 任務資料 CRUD
└── SubmissionRepository.js # 作品資料 CRUD
```

**說明**：
- 實作 **Repository Pattern**
- 集中管理資料庫查詢
- 易於測試和維護

#### `models/` - 資料模型
定義 MongoDB Schema 與資料結構。

```
models/
├── User.js        # 用戶模型
├── Chapter.js     # 章節模型
├── Task.js        # 任務模型
├── Submission.js  # 作品提交模型
├── Contact.js     # 聯絡表單模型
└── Analytics.js   # 分析數據模型 (Singleton Pattern)
```

**說明**：
- 使用 Mongoose ODM
- `Analytics.js` 實作 **Singleton Pattern**

#### `config/` - 設定檔
系統設定與環境配置。

```
config/
└── db.js  # MongoDB 連線設定
```

---

### 📄 文件與設計說明 (Documentation)

專案包含完整的技術文件：

```
114_web_final/
├── README.md          # 專案整體說明、安裝與使用指引
├── API_SPEC.md        # API 規格文件 (17 個端點)
├── ARCHITECTURE.md    # 系統架構圖與流程圖
├── DESIGN_SPEC.md     # 設計規格說明
├── CRUD_MAPPING.md    # CRUD 操作對應表
└── MVP_FEATURES.md    # MVP 功能列表
```

**說明**：
- `API_SPEC.md`：完整的 API 文件，包含請求/響應範例
- `ARCHITECTURE.md`：Mermaid 格式的架構圖和 CRUD 流程圖
- 所有文件使用 Markdown 格式，支援 GitHub 顯示

---

### 🗂️ 其他重要目錄與檔案

#### `scripts/` - 工具腳本
資料庫初始化與管理腳本。

```
scripts/
├── seed.js                  # 資料庫初始化 (建立章節和任務)
└── verify-existing-users.js # 批次驗證現有用戶
```

#### `uploads/` - 檔案儲存
用戶上傳的照片檔案。

```
uploads/
└── [timestamp]-[filename].jpg  # 上傳的照片
```

**說明**：
- 使用 Multer 處理檔案上傳
- 檔名格式：`時間戳-原檔名`

#### 根目錄檔案

```
114_web_final/
├── server.js        # Express 伺服器入口
├── package.json     # 專案套件管理與執行指令
├── .env             # 環境變數設定 (不納入版控)
├── .env.example     # 環境變數範例
├── .gitignore       # Git 版本控制忽略設定
├── promote_admin.js # 提升用戶為管理員的腳本
├── test_email.js    # Email 功能測試腳本
└── check_user_progress.js # 檢查用戶進度腳本
```

---

### 🏗️ 架構分層說明

```
┌─────────────────────────────────────────┐
│          前端 (Frontend)                 │
│  public/index.html + public/js/app.js   │
└─────────────────┬───────────────────────┘
                  │ HTTP Requests
┌─────────────────▼───────────────────────┐
│          路由層 (Routes)                 │
│     routes/auth.js, api.js, admin.js    │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│        中介層 (Middleware)               │
│  auth, admin, verifiedOnly, tracker     │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│        服務層 (Services)                 │
│  AuthService, ChapterService, etc.      │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│      資料存取層 (Repositories)           │
│  UserRepository, ChapterRepository      │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│         資料庫 (MongoDB)                 │
│  Users, Chapters, Tasks, Submissions    │
└─────────────────────────────────────────┘
```

---

### 📊 完整目錄樹

```
114_web_final/
├── config/
│   └── db.js                 # MongoDB 連線設定
├── middleware/
│   ├── auth.js              # JWT 認證 middleware
│   ├── admin.js             # 管理員權限檢查
│   ├── verifiedOnly.js      # Email 驗證檢查
│   └── pageViewTracker.js   # 頁面瀏覽追蹤
├── models/
│   ├── User.js              # 用戶模型
│   ├── Chapter.js           # 章節模型
│   ├── Task.js              # 任務模型
│   ├── Submission.js        # 作品提交模型
│   ├── Contact.js           # 聯絡表單模型
│   └── Analytics.js         # 分析數據模型
├── routes/
│   ├── auth.js              # 認證路由 (註冊/登入/驗證)
│   ├── api.js               # 主要 API 路由
│   └── admin.js             # 管理員路由
├── services/
│   ├── AuthService.js       # 認證服務
│   ├── ChapterService.js    # 章節服務
│   ├── TaskService.js       # 任務服務
│   ├── SubmissionService.js # 作品服務
│   └── EmailService.js      # Email 服務
├── repositories/
│   ├── UserRepository.js    # 用戶資料存取
│   ├── ChapterRepository.js # 章節資料存取
│   ├── TaskRepository.js    # 任務資料存取
│   └── SubmissionRepository.js # 作品資料存取
├── public/
│   ├── index.html           # 主頁面
│   ├── admin.html           # 管理員儀表板
│   ├── css/
│   │   └── styles.css       # 樣式表
│   └── js/
│       ├── app.js           # 主要前端邏輯
│       ├── eventBus.js      # 事件總線
│       └── componentFactory.js # 元件工廠
├── scripts/
│   ├── seed.js              # 資料庫初始化腳本
│   └── verify-existing-users.js # 用戶驗證腳本
├── uploads/                 # 上傳檔案目錄
├── utils/
│   └── logger.js            # 日誌工具
├── .env                     # 環境變數（不納入版控）
├── .env.example             # 環境變數範例
├── .gitignore               # Git 忽略設定
├── server.js                # 伺服器入口
├── package.json             # 專案設定
├── README.md                # 專案說明
├── API_SPEC.md              # API 規格文件
├── ARCHITECTURE.md          # 架構與流程圖
├── DESIGN_SPEC.md           # 設計規格
├── CRUD_MAPPING.md          # CRUD 對應表
├── MVP_FEATURES.md          # MVP 功能
├── promote_admin.js         # 管理員提升腳本
├── test_email.js            # Email 測試
└── check_user_progress.js   # 進度檢查
```

---

## 🔌 API 文件

### 認證相關

| Method | Endpoint | 說明 | 權限 |
|--------|----------|------|------|
| POST | `/api/auth/register` | 註冊新用戶 | Public |
| POST | `/api/auth/login` | 用戶登入 | Public |
| GET | `/api/auth/verify/:token` | Email 驗證 | Public |
| POST | `/api/auth/resend-verification` | 重發驗證郵件 | Public |
| GET | `/api/auth/me` | 取得當前用戶資料 | Private |

### 章節與任務

| Method | Endpoint | 說明 | 權限 |
|--------|----------|------|------|
| GET | `/api/chapters` | 取得所有章節 | Public |
| GET | `/api/chapters/:id` | 取得單一章節 | Public |
| GET | `/api/chapters/:id/tasks` | 取得章節任務 | Public |

### 作品管理

| Method | Endpoint | 說明 | 權限 |
|--------|----------|------|------|
| POST | `/api/submissions` | 提交作品 | Private + Verified |
| GET | `/api/submissions` | 取得所有作品 | Public |
| GET | `/api/submissions/user/:userId` | 取得用戶作品 | Public |
| PUT | `/api/submissions/:id` | 更新作品 | Private + Verified |
| DELETE | `/api/submissions/:id` | 刪除作品 | Private + Verified |

### 管理員

| Method | Endpoint | 說明 | 權限 |
|--------|----------|------|------|
| GET | `/api/admin/analytics` | 取得分析數據 | Admin |
| POST | `/api/analytics/click` | 記錄點擊事件 | Public |

---

## 🔐 管理員功能

### 訪問管理員儀表板

1. **建立管理員帳號**：
   ```bash
   node promote_admin.js <your-email>
   ```

2. **登入系統**

3. **訪問儀表板**：
   - 方式 1：點擊頁面頂部的「🔧 管理員」連結
   - 方式 2：直接訪問 `http://localhost:3001/admin.html`

### 儀表板功能

- **網站點擊次數**：追蹤所有用戶點擊互動
- **活躍帳戶**：30 天內登入的已驗證用戶
- **總註冊用戶**：所有註冊用戶數量
- **已驗證用戶**：Email 已驗證的用戶數

---

## ❓ 常見問題

### Q: 無法發送驗證郵件？

**A**: 請確認：
1. Gmail App Password 是否正確設定
2. `.env` 中的 `EMAIL_USER` 和 `EMAIL_PASS` 是否正確
3. Gmail 帳號是否啟用「兩步驟驗證」
4. 執行 `node test_email.js` 測試郵件功能

### Q: 章節頁面顯示「載入中...」？

**A**: 請嘗試：
1. 強制重新整理瀏覽器（Cmd+Shift+R）
2. 確認不在驗證頁面（URL 不含 `verify-email`）
3. 點擊「學習章節」或「PhotoMission」logo 返回首頁
4. 檢查瀏覽器 Console 是否有錯誤

### Q: 如何重置資料庫？

**A**: 
```bash
# 刪除資料庫
mongo photomission --eval "db.dropDatabase()"

# 重新初始化
npm run init-db
```

### Q: 忘記管理員密碼？

**A**:
```bash
# 重新設定為管理員
node promote_admin.js <your-email>
```

### Q: 上傳的照片儲存在哪裡？

**A**: 照片儲存在 `uploads/` 目錄中，檔名格式為 `{timestamp}-{originalname}`

---

## 📝 開發指令

```bash
# 安裝依賴
npm install

# 啟動開發伺服器（自動重啟）
npm run dev

# 啟動正式伺服器
npm start

# 初始化資料庫
npm run init-db

# 建立管理員
node promote_admin.js <email>

# 檢查用戶進度
node check_user_progress.js <email>

# 測試郵件功能
node test_email.js
```

---

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

---

## 📄 授權

ISC License

---

## 👨‍💻 作者

**Morris Lee ＆ Antigravity Team**

---

## 🙏 致謝

- Express.js 團隊
- MongoDB 團隊
- 所有開源貢獻者

---

**享受您的攝影學習之旅！📸**
